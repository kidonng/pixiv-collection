{
  "private": true,
  "scripts": {
    "serve": "yarn --cwd package/site serve",
    "build": "yarn --cwd package/site build",
    "now-dev": "yarn serve",
    "now-build": "yarn build"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "10.x"
  },
  "workspaces": [
    "package/*"
  ],
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
