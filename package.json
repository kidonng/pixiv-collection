{
  "private": true,
  "scripts": {
    "serve": "yarn --cwd site vue-cli-service serve",
    "build": "yarn --cwd site vue-cli-service build",
    "now-dev": "yarn serve",
    "now-build": "yarn build"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "10.x"
  },
  "workspaces": [
    "api",
    "site"
  ],
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
