{
  "private": true,
  "scripts": {
    "serve": "yarn --cwd packages/site vue-cli-service serve",
    "build": "yarn --cwd packages/site vue-cli-service build",
    "now-dev": "yarn serve",
    "now-build": "yarn build"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "10.x"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
